knitwear_recommender/             # Main project root folder
│
├── .github/                      # WEEK 1: Automation
│   └── workflows/
│       └── run_etl.yml           # GitHub Actions workflow to trigger Mage/data sync
│
├── .gitignore                    # Ignore local weights, credentials, and venvs
│
├── README.md                     # Documentation of the 4-week architecture
│
├── requirements.txt              # Unified local Python dependencies (PyTorch, Streamlit, etc.)
│
├── data_pipeline/                # WEEK 1: Mage Subfolder (Created automatically by Mage)
│   ├── data_loaders/
│   │   └── pattern_lists.py       # Python script hitting the Ravelry API
│   ├── data_exporters/
│   │   └── export_pattern_lists.py   # Script loading data to BigQuery
│   │   └── export_pattern_details.py   # Script loading data to BigQuery
│   │   └── dimensions.sql          #Creates primary lookup table
│   │   └── bridge_attributes.sql      # Creates list of patterns and attributes
│   │   └── pattern_photos.sql      # Creates a list of patterns and photo urls
│   ├── data_transformers/
│   │   └── export_pattern_details.py  # Retrieves from patterns using API
│   └── io_config.yaml           # Central connections profile (holds GCP key path & API keys)
│
├── ml_engine/                    # WEEK 2: Local Feature Extraction & Multicentroid Processing
│   ├── models/                   
│   │   ├── yolov8n-pose.pt       # Cached local YOLO model weights
│   │   └── dinov2_vitb14.pt      # Cached local DINOv2 model weights
│   ├── src/
│   │   ├── image_downloading.py  # Downloads images from Ravlery to GCS
│   │   ├── crop_images.py        # Cropping logic using YOLO-v8n-Pose points
│   │   ├── extract_features.py   # Image vector extraction via DINOv2
│   │   └── cluster_centroids.py  # K-Means clustering script to generate 3-5 centroids per pattern
│   └── generate_embeddings.py    # Main orchestration execution script for ML steps
│
├── sql_assets/                   # WEEKS 1, 2, & 3: Production Database Scripts
│   ├── staging/
│   │   └── flatten_ravelry_json.sql # ELT transformation query using JSON_EXTRACT_ARRAY & UNNEST
│   ├── indexing/
│   │   ├── text_embeddings_ml.sql   # BigQuery ML script calling Vertex AI text embedding model
│   │   └── create_vector_indexes.sql # CREATE VECTOR INDEX statements for text and image columns
│   └── procedures/
│       └── hybrid_search_engine.sql  # Complex Stored Procedure handling dual VECTOR_SEARCH & min aggregation
│
└── streamlit_app/                # WEEK 4: User Interface
    ├── assets/                   # UI design files, custom CSS, or logo images
    ├── app.py                    # Main application entrance handling file upload & weights
    └── bq_client.py              # Isolated module handling BigQuery Python SDK connection & logic